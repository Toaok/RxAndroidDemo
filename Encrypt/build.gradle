apply {
    from "${rootDir.path}/config_lib.gradle"
    plugin "com.android.library"
}

configAndroidModuleDomain project
configAppBaseDependences project

dependencies {
    implementation dependencesLibrary.commons_codec
}